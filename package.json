{
  "name": "chatto-enhancer",
  "version": "1.2",
  "description": "Cross-browser extension adding themes, volume sliders, emoji, GIFs, markdown controls, and more to Chatto.",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "node scripts/check.mjs",
    "build": "node scripts/build.mjs",
    "package": "node scripts/package.mjs",
    "test": "node --test test/*.test.mjs"
  },
  "license": "MIT",
  "engines": {
    "node": ">=20"
  }
}
